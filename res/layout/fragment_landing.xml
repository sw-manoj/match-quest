<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.development.teamquest.MainActivity$PlaceholderFragment" >

    <Button
        android:id="@+id/player_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/match_button"
        android:layout_alignBottom="@+id/match_button"
        android:layout_marginRight="36dp"
        android:layout_marginEnd="36dp"
        android:layout_toLeftOf="@+id/match_button"
        android:layout_toStartOf="@+id/match_button"
        android:text="@string/player_button" />

    <Button
        android:id="@+id/match_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="55dp"
        android:layout_marginRight="61dp"
        android:text="@string/match_button" />

</RelativeLayout>
